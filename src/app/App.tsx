import OpenLayersMap from '../widgets/OpenLayersMap/openLayersMap' 

const App = () => {
  return (
    <div style={{ width: '100vw', height: '100vh' }}>
      <OpenLayersMap />
    </div>
  )
}

export default App